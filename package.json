{
  "name": "serverless-kaban",
  "version": "1.0.0",
  "description": "Serverless Kaban REST API connected to Postgres DB",
  "main": "handler.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "serverless-pseudo-parameters": "^2.5.0"
  },
  "dependencies": {
    "aws-sdk": "^2.724.0",
    "mysql2": "^2.1.0",
    "pg": "^8.3.0"
  }
}
